# Copy this file to .env.local and set your own value.
# .env.local is gitignored — never commit real secrets.

# URL of the MeryFilms backend (see the separate backend/ project).
# Local dev default matches `npm run dev` in backend/. Set this to your
# deployed Render/Railway URL for production builds.
VITE_API_URL=http://localhost:4000
